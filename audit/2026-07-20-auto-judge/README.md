# 自動工程判定の実データ監査 (2026-07-20)

Phase A / A.1 で「自動工程かどうか」の判定を `src/domain/workExecution.js` の共通関数へ一本化した際の、
**本番実データによる影響調査**の記録です。第三者が同じ結果を再現できるように、使ったスクリプトと結果JSONをそのまま残します。

## 全部読み取り専用

`setDoc` / `updateDoc` / `deleteDoc` は import すらしていません（本番データを一切変更しません）。
Firestore の設定値は公開 web キー（クライアントに焼き込まれるもの）で、秘密情報ではありません。

## 再現方法

依存（`firebase`）の解決のため、**リポジトリのルート**から実行します。

```bash
cd product-inspection-app
node audit/2026-07-20-auto-judge/auto_judge_survey.mjs
```

| ファイル | 中身 |
|---|---|
| `auto_judge_survey.mjs` | 判定3種（①現行画面 ②現行🎯 ③新共通判定）の三者比較と🎯金額影響。**主スクリプト** |
| `auto_judge_survey.json` | 上の出力（案①＝マスタ参照なし の時点） |
| `verify2.json` | 案②（未設定工程は現行マスタの同名工程の明示設定を優先）で再実行した最終結果 |
| `lot_step_em.mjs` | ロット内に焼き込まれた工程の `executionMode` 分布（時期による判定割れの確認） |
| `full_time_survey.mjs` / `full_time_survey_result.json` | 製品・最終検査の実績時間の全量調査（2026-07-19） |
| `tpl_goal_verify.mjs` | 🎯集計を型式×テンプレ×工程にした際の検算 |

## 調査結果（`verify2.json` の実測値）

### 工程マスタ

| 項目 | 件数 |
|---|---|
| テンプレート数 | 26 |
| 工程総数 | **199** |
| `executionMode = 'auto'` | 0 |
| `executionMode = 'batch'` | 15 |
| `executionMode = 'manual'` | 101 |
| `executionMode` 未設定 | **83** |
| `rotaryLink = true` | **0** |

`rotaryLink` が0件なので、共通判定の②（rotaryLink→自動）は現時点では実データに影響しません。
未設定83件が名称推定または案②のマスタ参照にかかる対象です。

### 完了タスク

| 項目 | 値 |
|---|---|
| 完了タスク数 | 3,871 |
| 実績時間 合計 | 368.9 h |

### 🎯金額・機械時間への影響

| 指標 | 変更前 | 変更後 | 差 |
|---|---|---|---|
| 候補在庫（金額） | ¥1,013,899 | ¥1,013,899 | **±0** |
| 機械時間（年） | 698 h | **601 h** | −97 h |

金額が動かないのは、拘束率 `autoLaborPct` が現在 **100%**（＝自動工程の時間も全額を人件費に入れている上限設定）だからです。
拘束率を実態値へ下げた時点で、判定の違いが金額に効いてきます。

機械時間が 698h → 601h に減ったのは、案②（マスタ参照）により
「三次元測定開始」の旧ロット（`executionMode` 未設定で保存されていたもの）が、
現行マスタの明示設定 `manual` に揃って手動扱いになったためです。
旧判定では名称の「測定開始」だけを見て自動としていました。

この 601h / ¥1,013,899 は、アプリ画面の表示と一致することを確認済みです。
